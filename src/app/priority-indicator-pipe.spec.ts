import { PriorityIndicatorPipe } from './priority-indicator-pipe';

describe('PriorityIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
